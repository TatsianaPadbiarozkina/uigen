{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "$f = ($input | ConvertFrom-Json).tool_input.file_path; if ($f -and $f -match '\\.env') { Write-Error 'ACCESS DENIED: Это секретный файл!'; exit 1 }",
            "shell": "powershell"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "$f = ($input | ConvertFrom-Json).tool_input.file_path; if ($f) { npx prettier --write $f } else { exit 0 }",
            "shell": "powershell"
          }
        ]
      }
    ]
  }
}
